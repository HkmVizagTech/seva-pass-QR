# Backend URL used by the native Android build (vite build --mode android).
# - Emulator/device test with `adb reverse tcp:4000 tcp:4000`: http://localhost:4000
# - Production: use your deployed https backend, e.g. https://seva-pass-api.example.com
VITE_API_URL=https://seva-pass-server-production.up.railway.app
